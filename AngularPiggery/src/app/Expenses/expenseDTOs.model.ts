export interface expenseCreationDTO {
    date: Date;
    cost: number;
    notes: string;
}

export interface expenseDTO {
    id: number;
    date: string;
    cost: number;
    notes: string;
}